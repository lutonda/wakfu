<form method="post" action="{{ path('endereco_delete', {'id': endereco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ endereco.id) }}">
    <button class="btn">Delete</button>
</form>
