<form method="post" action="{{ path('contato_delete', {'id': contato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contato.id) }}">
    <button class="btn"><i class="fa fa-trash"></i></button>
</form>
