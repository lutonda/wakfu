<form method="post" action="{{ path('grau_academico_delete', {'id': grau_academico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grau_academico.id) }}">
    <button class="btn">Delete</button>
</form>
