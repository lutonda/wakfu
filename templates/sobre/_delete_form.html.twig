<form method="post" action="{{ path('sobre_delete', {'id': sobre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sobre.id) }}">
    <button class="btn">Delete</button>
</form>
