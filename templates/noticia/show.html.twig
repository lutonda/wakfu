{% extends 'base.html.twig' %}

{% block title %}Noticia{% endblock %}

{% block body %}
    <h1>Noticia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ noticium.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ noticium.titulo }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ noticium.categoria }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ noticium.text }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ noticium.code }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ noticium.created ? noticium.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ noticium.isactive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('noticia_index') }}">back to list</a>

    <a href="{{ path('noticia_edit', {'id': noticium.id}) }}">edit</a>

    {{ include('noticia/_delete_form.html.twig') }}
{% endblock %}
