<form method="post" action="{{ path('instituicionais_delete', {'id': instituicional.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instituicional.id) }}">
    <button class="btn">Delete</button>
</form>
